# writing a prog to find sum of arrays
def sumofarrays(self):
    sum = 0
    # initialize a variable
    # to store the sum
    # while iterating through
    # the array later
    for i in arr1:
        sum += i
    return (sum)


# Main Function
if __name__ == "__main__":

    # Defining array
    arr1 = [1, 2, 3, 4, 5]
    # calling function to calculate sum of array
    sar = sumofarrays(arr1)
    # Display sum
    print(sar)
