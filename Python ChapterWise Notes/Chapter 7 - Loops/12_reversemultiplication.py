number = int(input("Enter the numer: "))
for i in range (10,0,-1):
    print(str(number) + " X " + str(i) + " = ",number*i) # to print the variable in a print statement we can pass using str function
    # print(f"{number} X {i} = {number*i}") # we can use f " to write variables in a print statement using {} curly brace. to enter any sign of =/-+* we can use without pasisng then in between double quotes "" "
